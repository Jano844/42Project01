#include <stdlib.h>
#include <stdio.h>
#include <string.h>

char *ft_substr(char const *s, unsigned int start, size_t len)
{
	if (!s && start && len)
		return (NULL);
	return (NULL);
}

// int main ()
// {

// }
